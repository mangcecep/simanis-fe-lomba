# Base URL API backend SIMANIS
# Mode lokal (backend dijalankan dengan Laragon / npm run dev):
VITE_API_BASE_URL=http://localhost:8000/api

# Mode deployed (backend di-expose via ngrok):
# Hapus/komentari baris VITE_API_BASE_URL di atas,
# maka api.ts otomatis memakai default ngrok.